This is the github page for the simulator 
https://github.com/RoboCup-SSL/grSim.git

before you start following the download instructions
make sure you run these two commands

sudo apt update
sudo apt install

First command to list all the avilable updates.
Second command to install all the updates.
